# Desktop: omit VITE_API_URL — the app uses http://localhost:3000.
#
# Mobile (same Wi-Fi): put this in .env.local (not committed), with your current IP:
# VITE_API_URL=http://<your-ip>:3000
#
# Production (Vercel): point at Render API
# VITE_API_URL=https://workoutdb-api.onrender.com
